@page "/add-company"
@using RoaringView.Model
@using RoaringView.Pages

<h3>Add Company</h3>

<div class="container-fluid">
    <form>
        @if (!isAdvancedSearch)
        {
            <!-- Basic Search -->
            <div class="row">
                <div class="form-group col-md-12">
                    <label for="orgNumberInputBasic">Organization Number</label>
                    <input type="text" class="form-control" id="orgNumberInputBasic" @bind="FreeText" placeholder="Enter organization number">
                </div>
            </div>
        }
        else
        {
            <!-- Advanced Search -->

            <div class="row">
            <div class="form-group col-md-6">
                <label for="orgNumberInput1">Organization Number</label>
                <input type="text" class="form-control" id="orgNumberInput1" @bind="FreeText" placeholder="Enter organization number">
            </div>
          @*   <div class="form-group col-md-6">
                <label for="orgNumberInput2">Organization Number</label>
                <input type="text" class="form-control" id="orgNumberInput2" @bind="FreeText" placeholder="Enter organization number">
            </div> *@
        </div>
     @*    <div class="row">
            <div class="form-group col-md-4">
                <label for="orgNumberInput3">Organization Number</label>
                <input type="text" class="form-control" id="orgNumberInput3" @bind="FreeText" placeholder="Enter organization number">
            </div>
            <div class="form-group col-md-4">
                <label for="orgNumberInput3">Organization Number</label>
                <input type="text" class="form-control" id="orgNumberInput3" @bind="FreeText" placeholder="Enter organization number">
            </div>
            <div class="form-group col-md-4">
                <label for="orgNumberInput3">Organization Number</label>
                <input type="text" class="form-control" id="orgNumberInput3" @bind="FreeText" placeholder="Enter organization number">
            </div>
        </div> *@

        }
        <button type="button" class="btn btn-primary" @onclick="SearchCompany">Search</button>
    </form>

    <button type="button" class="btn btn-link" @onclick="ToggleSearchMode">
        @if (isAdvancedSearch)
        {
            <span>Basic Search</span>
        }
        else
        {
            <span>Advanced Search</span>
        }
    </button>
</div>

@if (SearchResult != null)
{
    @DisplayCompaniesTable
}



@code {
    private bool isAdvancedSearch = false; // This variable tracks the search mode

    private void ToggleSearchMode()
    {
        isAdvancedSearch = !isAdvancedSearch; // Toggle the search mode
    }
}