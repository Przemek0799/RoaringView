@page "/login"
@attribute [AllowAnonymous]


<section class="vh-100">
    <div class="container-fluid">
        <div class="row">
            <div class="  col-md-7 col-lg-6 col-xl-5 col-xxl-4 text-black">
               
                <!-- Login Form -->
                <div class="px-5 ms-xl-4" style="margin-top: 2rem;">
                    <form style="width: 23rem;" @onsubmit="LoginClicked">
                        <h3 class="text-center mb-3 pb-3" style="letter-spacing: 1px;">Log in</h3>

                        <!-- Email Input -->
                        <div class="form-outline mb-3">
                            <input type="email" class="form-control form-control-lg" @bind="Email" />
                            <label class="form-label" >Email address</label>
                        </div>

                        <!-- Password Input -->
                        <div class="form-outline mb-3">
                            <input type="password" class="form-control form-control-lg" @bind="password" />
                            <label class="form-label" >Password</label>
                        </div>

                        <!-- Error Message -->
                        @if (!string.IsNullOrEmpty(error))
                        {
                            <div class="alert-danger">
                                <p>@error</p>
                            </div>
                        }

                        <!-- Login Button -->
                        <div class=" text-center pt-1 mb-4">
                            <button class="btn custom-button-color btn-lg btn-block" type="submit">Login</button>
                        </div>

                      @*  Lägg till logga in med google?  *@
                        <!-- Register buttons -->
                        <div class="text-center">
                            <p>or sign up with:</p>
                            <button type="button" class="btn btn-link btn-floating mx-1">
                                <i class="fab fa-facebook-f"></i>
                            </button>

                            <button type="button" class="btn btn-link btn-floating mx-1">
                                <i class="fab fa-google"></i>
                            </button>

                            <button type="button" class="btn btn-link btn-floating mx-1">
                                <i class="fab fa-twitter"></i>
                            </button>

                            <button type="button" class="btn btn-link btn-floating mx-1">
                                <i class="fab fa-github"></i>
                            </button>
                        </div>
                       
                    </form>
                 
                </div>
            </div>

            <!-- Image Section -->
            <div class="col-md-5 col-lg-6 col-xl-7 col-xxl-8 d-none d-md-block">
                <img src="https://picsum.photos/1280/1024"
                     alt="Login image" class="w-100 vh-100" style="object-fit: cover; ">
            </div>
        </div>
    </div>
</section>
